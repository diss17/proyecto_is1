# Modelo Java generado

Este directorio mantiene la documentación del modelo. Las clases Java viven en `src/main/java/model` y fueron generadas a partir de los diagramas conceptuales y de secuencia del sistema de recolección. Cada clase representa una entidad del modelo y sus relaciones principales.
